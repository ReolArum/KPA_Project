# 2026-04-05 작업 내역

## 진행 완료된 작업 (Tasks Completed)

- **[탐사 데이터 구조 분석]**: `ExplorationStageData.cs`의 모든 필드(Node, Choice, Requirement 등)에 대한 상세 설명 완료.
- **[Clue 노드 로직 분석]**: 단서 수집 시의 자동 획득 방식 및 `interactionRange`와의 차이점 정리.
- **[알림 UI 구현]**: 
    - `ExplorationUIController.cs`에 `textActionResult` 필드 및 표시 로직 추가.
    - 단서 발견 및 패널티 발생 시 화면 중앙에 2.5초간 메시지가 표시되도록 코루틴 구현.
- **[최신 API 대응 및 버그 수정]**:
    - `ExplorationManager.cs`: 더 이상 사용되지 않는 `FindFirstObjectByType`, `FindObjectsByType(FindObjectsSortMode)` 호출을 최신 방식(`FindAnyObjectByType`, `FindObjectsInactive.Exclude`)으로 교체.
    - `DisableTextRayCast.cs`: 위와 동일한 Deprecated 경고 해결 및 `FindObjectsInactive.None` 컴파일 에러 수정.
- **[시스템 최적화 및 코드 정리]**:
    - **UI 매핑 버그 수정**: 3D 월드 좌표를 UI 스크린 좌표로 변환하여 지도 상의 노드 아이콘 배치를 정확하게 수정.
    - **성능 최적화**: 매 프레임 모든 노드를 전수 조사하던 `ScanNearbyNodes` 로직을 거리 기반(0.2m 이동 시) 트리거 방식으로 개선.
    - **렌더러 캐싱**: 스테이지 시작 시 주변 렌더러를 미리 캐싱하여 하이라이트 연산 시의 부하(`OverlapSphere`) 제거.
    - **레거시 제거**: New Input System 전용으로 코드를 정리하고 중복된 비주얼 동기화 로직을 `SyncVisualPosition`으로 통합.
- **[전체 코드 안정화]**: 씬 마커 스캔 중복 선언 제거 및 탐사 시스템 전반의 컴파일 에러 해결.

---
*참고: 유니티 에디터 상에서 `ExplorationUIController`의 `Text Action Result` 필드에 알림용 TMP 텍스트 오브젝트를 할당해야 정상 작동합니다.*
